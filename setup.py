#!/usr/bin/env python
"""
Entry point for build tool setuptools
In this project the `setup()` will remain empty,
it is only needed to allow for editable install,
i.e. put link to package source code to not re-
install if package source is changed.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
